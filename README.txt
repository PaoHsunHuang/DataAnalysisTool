The program is running in Python 3.8.2. required modules include numpy, pandas, matplotlib,and tkinter. Modules can be installed by pip.
 The program require csv file as input and it will read the data inside automatic. The features in this program include sort, select, plotgraph and modeling  

